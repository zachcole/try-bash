var React = require('react');
var directionsComponent = require('../styles').directionsComponent;

var Directions = React.createClass({
	render: function () {
		return (
			<div style={directionsComponent}>Test</div>
		)
	}
});

module.exports = Directions;